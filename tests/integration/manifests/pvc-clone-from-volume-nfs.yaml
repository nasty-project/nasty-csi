apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-clone-nfs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nasty-csi-nfs
  dataSource:
    name: test-pvc-source-nfs
    kind: PersistentVolumeClaim
