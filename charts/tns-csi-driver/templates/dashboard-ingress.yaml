{{- if and .Values.controller.dashboard.enabled .Values.controller.dashboard.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tns-csi-driver.fullname" . }}-dashboard
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "tns-csi-driver.labels" . | nindent 4 }}
  {{- with .Values.controller.dashboard.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.controller.dashboard.ingress.className }}
  ingressClassName: {{ .Values.controller.dashboard.ingress.className }}
  {{- end }}
  {{- if .Values.controller.dashboard.ingress.tls }}
  tls:
    {{- range .Values.controller.dashboard.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.controller.dashboard.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "tns-csi-driver.fullname" $ }}-dashboard
                port:
                  name: dashboard
          {{- end }}
    {{- end }}
{{- end }}
